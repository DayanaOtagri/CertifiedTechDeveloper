    /* function prueba (x) {
        if (isNaN(x)){
            return "No es un número";
        }
        return "Es un número";
    }

    console.log(prueba("T"));*/ 

    // Es una funcion que comprueba si el parametro es un número o no 
    // retorna False o True según corresponda. 

    /* isNaN(); 
    console.log(isNaN("t")); */

// Se puede poner el número en un String y lo va a validar como un número

/*for (let numero = 0; numero <= 10; numero++){
    console.log(`El número es ${numero}`)  
};*/

let numero = 1
while (numero <= 10){
    console.log(`El número es ${numero}`)  
    numero++
}



   /* function milliseconds(x) {
    if (isNaN(x)) {
      return 'Not a Number!';
    }
    return x * 1000;
  }
  
  console.log(milliseconds('100F')); */ 